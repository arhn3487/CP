#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    ll n,k,cnt=0;
    vector<ll> v;
    cin>>n>>k;

    for(ll i=1;i<=sqrt(n);i++)
    {
        if(n%i==0)
        {
           if(i!=n)
        }
    }
    cout<<-1<<endl;
}